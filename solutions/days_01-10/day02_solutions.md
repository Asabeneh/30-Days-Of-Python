# ✅ حلول اختبار اليوم الثاني: المتغيرات والدوال المدمجة

<div dir="rtl">

## القسم الأول: أسئلة الاختيار من متعدد

### السؤال 1 ✅
**الإجابة الصحيحة:** C) my_var

**الشرح:** قواعد تسمية المتغيرات في Python:
- ✅ يمكن أن تحتوي على حروف، أرقام، وشرطة سفلية (_)
- ✅ يجب أن تبدأ بحرف أو شرطة سفلية
- ❌ لا يمكن أن تبدأ برقم (2myvar)
- ❌ لا يمكن أن تحتوي على شرطة (-) أو مسافات

أمثلة صحيحة: `my_var`, `_private`, `myVar2`, `MY_CONSTANT`
أمثلة خاطئة: `2myvar`, `my-var`, `my var`, `my.var`

---

### السؤال 2 ✅
**الإجابة الصحيحة:** B) 10

**الشرح:** هذه طريقة أنيقة لتبديل قيم المتغيرات في Python:
```python
x = 5
y = 10
x, y = y, x  # تبديل القيم
print(x)  # 10
print(y)  # 5
```
بعد التبديل، x أصبحت 10 و y أصبحت 5

---

### السؤال 3 ✅
**الإجابة الصحيحة:** B) input()

**الشرح:** الدالة `input()` تُستخدم لقراءة مدخلات المستخدم:
```python
name = input("ما اسمك؟ ")
print("مرحباً", name)
```
ملاحظة مهمة: `input()` تُرجع دائماً string!

---

### السؤال 4 ✅
**الإجابة الصحيحة:** B) 6

**الشرح:** الدالة `len()` تحسب عدد العناصر أو الأحرف:
```python
print(len("Python"))  # 6 حروف
print(len([1,2,3]))   # 3 عناصر
```
"Python" تحتوي على 6 حروف: P-y-t-h-o-n

---

### السؤال 5 ✅
**الإجابة الصحيحة:** A) int("123")

**الشرح:** دوال التحويل في Python:
```python
int("123")      # تحويل إلى integer → 123
float("3.14")   # تحويل إلى float → 3.14
str(123)        # تحويل إلى string → "123"
```

---

### السؤال 6 ✅
**الإجابة الصحيحة:** B) float

**الشرح:**
```python
print(type(3.14))  # <class 'float'>
print(type(3))     # <class 'int'>
print(type("3"))   # <class 'str'>
```

---

## القسم الثاني: صح أو خطأ

### السؤال 7 ✅
**الإجابة الصحيحة:** خطأ ✗

**الشرح:** اسم المتغير **لا يمكن** أن يبدأ برقم:
```python
# خطأ
2myvar = 10  # SyntaxError

# صحيح
myvar2 = 10
_2myvar = 10
```

---

### السؤال 8 ✅
**الإجابة الصحيحة:** صح ✓

**الشرح:** Python حساسة لحالة الأحرف (Case-sensitive):
```python
name = "Ahmed"
Name = "Ali"
NAME = "Hassan"

# هذه ثلاث متغيرات مختلفة!
print(name)  # Ahmed
print(Name)  # Ali
print(NAME)  # Hassan
```

---

### السؤال 9 ✅
**الإجابة الصحيحة:** صح ✓

**الشرح:** الدالة `input()` تُرجع دائماً string:
```python
age = input("كم عمرك؟ ")  # إذا أدخلت 25
print(type(age))  # <class 'str'> وليس int!

# يجب التحويل:
age = int(input("كم عمرك؟ "))
print(type(age))  # <class 'int'>
```

---

### السؤال 10 ✅
**الإجابة الصحيحة:** صح ✓

**الشرح:** يمكن إسناد قيم لعدة متغيرات في سطر واحد:
```python
# نفس القيمة لعدة متغيرات
x = y = z = 10

# قيم مختلفة
a, b, c = 1, 2, 3

# التبديل
x, y = y, x
```

---

## القسم الثالث: أكمل الكود

### السؤال 11 ✅
**الإجابة الصحيحة:** `type`

**الكود الكامل:**
```python
x = 10
print(type(x))  # <class 'int'>
```

---

### السؤال 12 ✅
**الإجابة الصحيحة:** `float`

**الكود الكامل:**
```python
num = float("3.14")
print(num)       # 3.14
print(type(num)) # <class 'float'>
```

---

### السؤال 13 ✅
**الإجابة الصحيحة:** `len`

**الكود الكامل:**
```python
my_list = [1, 2, 3, 4, 5]
length = len(my_list)
print(length)  # 5
```

---

### السؤال 14 ✅
**الإجابة الصحيحة:** `=` و `=`

**الكود الكامل:**
```python
a = b = c = 10
print(a, b, c)  # 10 10 10
```

---

### السؤال 15 ✅
**الإجابة الصحيحة:** `max`

**الكود الكامل:**
```python
print(max(10, 20, 5, 30))  # 30

# دوال مشابهة:
print(min(10, 20, 5, 30))  # 5
print(sum([10, 20, 5, 30])) # 65
```

---

## القسم الرابع: تحليل الكود

### السؤال 16 ✅
**الإجابة الصحيحة:** `Ahmed Ali`

**الشرح:**
```python
first_name = "Ahmed"        # "Ahmed"
last_name = "Ali"           # "Ali"
full_name = first_name + " " + last_name  # "Ahmed" + " " + "Ali"
print(full_name)            # Ahmed Ali
```
عملية `+` مع strings تسمى concatenation (دمج النصوص)

---

### السؤال 17 ✅
**الإجابة الصحيحة:** `53`

**الشرح:**
```python
a = "5"  # string
b = "3"  # string
print(a + b)  # "53" (دمج نصوص وليس جمع أرقام!)

# للجمع الحسابي:
a = 5  # int
b = 3  # int
print(a + b)  # 8
```

---

### السؤال 18 ✅
**الإجابة الصحيحة:** `2`

**الشرح:**
```python
x = 5
y = 2
print(x // y)  # 2 (قسمة صحيحة - يحذف الباقي)
print(x / y)   # 2.5 (قسمة عادية)
print(x % y)   # 1 (باقي القسمة)
```

---

## القسم الخامس: أسئلة برمجية عملية

### السؤال 19 ✅

**الحل:**
```python
# الطريقة الأولى
name = input("ما اسمك؟ ")
print("مرحباً " + name)

# الطريقة الثانية
name = input("ما اسمك؟ ")
print("مرحباً", name)

# الطريقة الثالثة (f-string - Python 3.6+)
name = input("ما اسمك؟ ")
print(f"مرحباً {name}")

# الطريقة الرابعة (format)
name = input("ما اسمك؟ ")
print("مرحباً {}".format(name))
```

**مثال تشغيل:**
```
ما اسمك؟ محمد
مرحباً محمد
```

---

### السؤال 20 ✅

**الحل:**
```python
# الحل الأساسي
length = float(input("أدخل الطول: "))
width = float(input("أدخل العرض: "))
area = length * width
print("المساحة =", area)

# حل أفضل مع تنسيق
length = float(input("أدخل طول المستطيل: "))
width = float(input("أدخل عرض المستطيل: "))
area = length * width
print(f"مساحة المستطيل = {area}")

# حل متقدم مع معالجة أخطاء بسيطة
try:
    length = float(input("أدخل الطول: "))
    width = float(input("أدخل العرض: "))
    area = length * width
    print(f"المساحة = {area:.2f}")  # رقمين عشريين
except ValueError:
    print("خطأ! يرجى إدخال أرقام صحيحة")
```

**مثال تشغيل:**
```
أدخل الطول: 5
أدخل العرض: 3
المساحة = 15.0
```

---

## ملخص الدوال المدمجة المهمة

### دوال الإدخال والإخراج
```python
print()      # طباعة
input()      # قراءة مدخل
```

### دوال التحويل
```python
int()        # تحويل لـ integer
float()      # تحويل لـ float
str()        # تحويل لـ string
bool()       # تحويل لـ boolean
```

### دوال المعلومات
```python
type()       # معرفة نوع البيانات
len()        # حساب الطول
```

### دوال رياضية
```python
abs()        # القيمة المطلقة
max()        # أكبر قيمة
min()        # أصغر قيمة
sum()        # مجموع القيم
round()      # التقريب
pow()        # الأس
```

---

## نصائح مهمة

### 1. تسمية المتغيرات
```python
# جيد
user_name = "Ahmed"
user_age = 25
total_price = 100.5

# سيء (غير واضح)
un = "Ahmed"
ua = 25
tp = 100.5
```

### 2. تحويل الأنواع
```python
# خطأ شائع
age = input("العمر: ")
next_year = age + 1  # خطأ! لا يمكن جمع string و int

# الصحيح
age = int(input("العمر: "))
next_year = age + 1  # صحيح
```

### 3. دمج النصوص
```python
name = "Ahmed"
age = 25

# طرق مختلفة
print("اسمي " + name + " وعمري " + str(age))  # يجب تحويل age لـ string
print("اسمي", name, "وعمري", age)              # أسهل
print(f"اسمي {name} وعمري {age}")              # الأفضل (f-string)
```

---

## توزيع الدرجات

- القسم الأول: 30 نقطة
- القسم الثاني: 20 نقطة
- القسم الثالث: 25 نقطة
- القسم الرابع: 15 نقطة
- القسم الخامس: 10 نقطة

**الدرجة الكلية:** 100 نقطة

---

## نصائح للتحسين

### 90-100: ممتاز! 🌟
أنت تفهم المتغيرات والدوال المدمجة بشكل ممتاز. استمر!

### 70-89: جيد جداً! ⭐
راجع الأجزاء الصعبة وتدرب أكثر على التحويل بين الأنواع.

### 60-69: جيد! ✨
ركز على:
- قواعد تسمية المتغيرات
- الفرق بين أنواع البيانات
- استخدام دوال التحويل

### أقل من 60: يحتاج مراجعة! 📚
- أعد قراءة الدرس الثاني
- جرب جميع الأمثلة بنفسك
- اكتب برامج بسيطة للتدريب

---

**مبروك على إكمال اختبار اليوم الثاني! 🎉**

**انتقل للدرس التالي:** اليوم 03 - المعاملات (Operators)

</div>
